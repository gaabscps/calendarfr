# Dependencies
node_modules/

# Build outputs
dist/
build/
coverage/
.next/
.swc/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# Env
.env
.env.local
.env.*.local
.env.e2e

# App data (planner days)
data/

# Brainstorming companion
.superpowers/

# SDD pipeline state (per-feature; framework-internal)
.agent-session/

# Test artifacts
playwright-report/
test-results/

# Workspace build outputs (workspace-scoped to avoid matching root dist/ twice)
web/dist/
server/dist/

# Storybook static build
storybook-static/

# Bundler / tool caches
**/.cache/
**/.vite/
**/.swc/

# Quality tool reports (gitignored — only docs/agentops/ aggregates are committed)
reports/
